# fabuline-web

Site public de **Fabuline** (landing + pages légales), servi via **GitHub Pages**.

> Source de vérité : le dossier `web-pages/` du dépôt applicatif `historia-app`.
> Pour redéployer : recopier le contenu de `web-pages/` ici, puis `git add -A && git commit && git push`.
